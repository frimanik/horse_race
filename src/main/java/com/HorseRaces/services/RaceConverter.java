/*package com.HorseRaces.services;

import com.HorseRaces.entity.DB.RaceDB;
import com.HorseRaces.entity.Horse;
import com.HorseRaces.entity.Race;
import com.HorseRaces.repository.HorseRepository;
import com.HorseRaces.repository.RaceRepository;
import org.springframework.beans.factory.annotation.Autowired;

import java.util.List;

public class RaceConverter {

    @Autowired
    private HorseRepository horseRepository;

    @Autowired
    private RaceRepository raceRepository;


    public Race convertToRace(RaceDB raceDB){
        long id = raceRepository.
        List<Race> horses =raceRepository.get();
        return new Race(id,date,horses);
    }
}
*/